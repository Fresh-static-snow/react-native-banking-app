import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  SuccessContainer: {
    backgroundColor: 'green',
    padding: 3,
    paddingVertical: 3,
    borderRadius: 10,
  },
  SuccessText: {
    color: 'white',
    textAlign: 'center',
  },
});
